package model;

public class Canon extends Objet {

    protected double force;

    public Canon(double x, double y, double largeur, double hauteur, double angle) {
        super(x, y, largeur, hauteur, angle);
        force = 0; // a definir plus tard
    }

    // ---------GETTER SETTER---------
    // ---------GETTER SETTER---------
}
